server:
  port: 8070

eureka:
  instance:
    hostname: eurekaserver   # not localhost in Docker

  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      # override the default 8761 URL
      defaultZone: http://eurekaserver:8070/eureka/
      # or "" if you really want zero peers
